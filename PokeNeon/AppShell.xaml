<?xml version="1.0" encoding="utf-8" ?>
<Shell xmlns="http://xamarin.com/schemas/2014/forms"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:PokeNeon.Views"
       x:Class="PokeNeon.AppShell"
       TabBarBackgroundColor="#7E7E7E"
       NavBarIsVisible="False">

    <Shell.Resources>
        <ResourceDictionary>
            <Style x:Key="BaseStyle" TargetType="Element">
                <Setter Property="Shell.TitleColor" Value="White" />
                <Setter Property="Shell.UnselectedColor" Value="Black" />
            </Style>
            <Style TargetType="TabBar"  BasedOn="{StaticResource BaseStyle}" />
        </ResourceDictionary>
    </Shell.Resources>

    <TabBar>
        <Tab Icon="accueil.png"
             Title="Accueil">
            <views:HomePage/>
        </Tab>
        <Tab Icon="poke_ball.png"
             Title="Pokédex">
            <views:ListPage />
        </Tab>
        <Tab Icon="ajout.png"
             Title="Ajouter">
            <views:AddPage/>
        </Tab>
        <Tab Icon="recherche.png"
             Title="Rechercher">
            <views:SearchPage/>
        </Tab>
    </TabBar>
</Shell>